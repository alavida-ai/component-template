# Component Configuration
COMPONENT_NAME=my-component

# Platform internal auth (service-to-service)
PLATFORM_INTERNAL_SECRET=dev-secret-change-in-production
PLATFORM_API_URL=http://localhost:3000

# Anthropic API (if component uses Agent SDK)
ANTHROPIC_API_KEY=sk-ant-...

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Inngest
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=
INNGEST_DEV=1

# Logging
LOG_LEVEL=INFO

# Server
PORT=8000
